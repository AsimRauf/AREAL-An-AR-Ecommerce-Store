version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        # Environment mappings here
    build:
      commands:
        - npm run build || exit 1
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*